package c.teamtreehouse.foodieappcardview;import android.support.v7.app.AppCompatActivity;import android.os.Bundle;import com.bumptech.glide.Glide;import android.content.Intent;import android.widget.ImageView;import android.widget.TextView;public class MySecondActivity extends AppCompatActivity {    private String desc=" ";    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_my_second);        Intent intent = getIntent();        ImageView imageView = (ImageView)findViewById(R.id.imageView_mealsDetail);        TextView textView = findViewById(R.id.display_details);        desc = intent.getExtras().getString("desc");        textView.setText(desc);        //textView.setText(getResources().getString(R.string.sports_details_placeholder));        Glide.with(this)                .load(getIntent().getIntExtra("IMAGE_ID", 0))                .into(imageView);    }}